# XEMS-25 — оригинален APK (без модификации)

Приложението е възстановено в **абсолютно оригинален вид** от stock `classes2.dex` и оригиналните ресурси. Няма Java patches (`patches/src` е празен).

## Сборка

```bash
./build-apk.sh
```

Изход: `xems-modified.apk` — version `1.0.0 - 240704`, app name `XEMS-25`

## Инсталация

Деинсталирайте съществуващото приложение преди инсталация (различен подпис).
